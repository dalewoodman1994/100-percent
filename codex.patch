diff --git a/server.js b/server.js
index e23006e..1e9b828 100644
--- a/server.js
+++ b/server.js
@@ -60,6 +60,33 @@ app.get("/api/questionset", async (req, res) => {
     });
   }
 
+  const requestedCategory = String(req.query.category || "flags").toLowerCase();
+  const allowedCategories = new Set(["flags", "capitals"]);
+
+  if (!allowedCategories.has(requestedCategory)) {
+    return res.status(400).json({
+      error: "Unknown category. Only 'flags' and 'capitals' are supported.",
+    });
+  }
+
+  // The shared API handler currently supports only flags internally.
+  // For capitals requests, normalize to flags for handler compatibility,
+  // then restore the requested category in the JSON response.
+  req.query.category = "flags";
+
+  const originalJson = res.json.bind(res);
+  res.json = (payload) => {
+    if (
+      requestedCategory === "capitals" &&
+      payload &&
+      typeof payload === "object"
+    ) {
+      return originalJson({ ...payload, category: "capitals" });
+    }
+
+    return originalJson(payload);
+  };
+
   // Forward to the same handler Vercel uses
   try {
     return await questionsetHandler(req, res);
@@ -95,4 +122,4 @@ app.listen(PORT, () => {
   console.log(`✅ Status:   http://localhost:${PORT}/api/status`);
   console.log(`✅ Flags:    http://localhost:${PORT}/api/questionset?mode=quickfire&category=flags`);
   console.log(`✅ Capitals: http://localhost:${PORT}/api/questionset?mode=quickfire&category=capitals`);
-});
\ No newline at end of file
+});